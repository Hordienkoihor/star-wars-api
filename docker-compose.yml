version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swapi_api
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=dev
      - DB_URI=